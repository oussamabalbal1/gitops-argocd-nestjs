apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.ingress.name }}
  namespace: production
  annotations:
    external-dns.alpha.kubernetes.io/hostname: rr.381492032511.realhandsonlabs.net
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - rr.381492032511.realhandsonlabs.net
    secretName: tls-secret
  rules:
  - host: rr.381492032511.realhandsonlabs.net
    http:
      paths:
      {{- range .Values.ingress.paths }}
      - path: {{ .backend.path }}
        pathType: Prefix
        backend:
          service:
            name: {{ .backend.service.name | quote}}
            port:
              number: {{ .backend.service.port_number }}
      {{- end}}

